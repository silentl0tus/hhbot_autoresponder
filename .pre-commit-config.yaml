repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.0
    hooks:
      - id: ruff
        args: [ --fix ]
      - id: ruff-format
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: bash -c 'cd hh-autoresponder && ../.venv/bin/python -m pytest'
        language: system
        types: [python]
        pass_filenames: false
